
@model IEnumerable<Tarea2JonathanRojas.Models.ServPorEdif> 

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">

    <div class="row">
        <div class="col-sm-6">
            <h3>Lista de Servicios por edificio</h3>
        </div>
        <div class="col-sm-6">
            <a class="btn btn-success d-block" asp-action="Create" asp-controller="SPEController1">Agregar Servicio a Edificio</a>
        </div>

    </div>

    <div class="row">
            @if (Model.Count() > 0)
            {
                <table class="table table-bordered table-striped">
                    <thead>
                        <tr>
                            <td>@Html.DisplayNameFor(m => m.Id)</td>
                            <td>@Html.DisplayNameFor(m => m.NombreEdificio)</td>
                            <td>@Html.DisplayNameFor(m => m.Servicios)</td>
                            <td>@Html.DisplayNameFor(m => m.EmpresaServicio)</td>
                            <td>@Html.DisplayNameFor(m => m.FechaAdquisicion)</td>
                            <td>Acciones</td>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Id</td>
                                <td>@item.NombreEdificio</td>
                                <td>@item.Servicios</td>
                                <td>@item.EmpresaServicio</td>
                                <td>@item.FechaAdquisicion</td>
                                <td>
                                    <a asp-controller="SPEController1" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning">Editar</a>
                                    <a asp-controller="SPEController1" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Borrar</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else 
            {
                <p>No existen datos</p>
            }
        </div>

</div>